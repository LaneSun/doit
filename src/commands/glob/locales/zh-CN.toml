[glob]
skill = """
文件模式匹配。查找匹配 glob 模式的文件。

用法：
  doit glob '<模式>'
  doit glob --cwd <目录> '<模式>'

参数：
  pattern  - Glob 模式（如 "*.rs"、"src/**/*.rs"）
  --cwd    - 基础目录（默认为当前目录）

输出：
  每行一个文件路径，相对于基础目录。

场景：
  LLM 使用 glob 在操作前发现匹配模式的文件。比 find 或 ls 模式更快更可靠。
"""
