[prompt]
not_available = "doit prompt 仅在交互模式下可用"
skill = """
向用户提问并阻塞,直到用户回复。

用法：
  doit prompt '<消息>'

消息会展示给用户,随后出现输入提示等待一行输入。用户的回复作为命令结果返回。

场景：
  通常你直接用纯文本(不带工具调用)回复来和用户对话。仅当你需要在一连串命令
  中途向用户提问、又不想结束本回合时,才显式调用 doit prompt。仅交互模式可用。
"""
