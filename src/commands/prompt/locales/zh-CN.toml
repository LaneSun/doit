[prompt]
not_available = "doit prompt 仅在交互模式下可用"
skill = """
向用户提问并阻塞,直到用户回复。

用法：
  doit prompt '<消息>'

消息会打印给用户,随后出现 '> ' 提示等待一行输入。用户的回复作为命令结果返回。

场景：
  交互模式下,你通常直接用纯文本(不带工具调用)回复来和用户对话,系统会自动
  将其转换为一次提问。当你想在一连串命令中途向用户提问时,可显式调用 doit prompt。
  仅在交互模式下有意义。
"""
