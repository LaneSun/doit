[exit]
skill = """
完成当前任务并退出。

用法：
  doit exit '<总结>'
  doit exit <<'EOF'
多行总结第一行
多行总结第二行
EOF

输出：
  （无）

场景：
  LLM 在任务完成且无需进一步操作时调用 exit。总结传递给父 Agent 循环用于向用户报告。

仅在非交互模式（doit run / doit task）下可用。
"""
